export type CreateAddressProps = {
  first_name: string;
};

export interface Errors {
  firstName: string;
  lastName: string;
  mobilePhone: string;
  phoneNumber: string;
  address: string;
  gender: string;
}
